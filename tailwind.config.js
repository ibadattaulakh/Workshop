/** @type {import('tailwindcss').Config} */
export default {
  // In Tailwind v4, colors and fonts defined in @theme are automatically available
  // No need to extend theme here when using @theme directive
};

